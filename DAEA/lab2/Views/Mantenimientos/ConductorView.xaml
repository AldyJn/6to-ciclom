<UserControl x:Class="lab2.Views.Mantenimientos.ConductorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Registro de Conductor" Style="{StaticResource Titulo}"/>

        <StackPanel Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Left" Width="320">
            <TextBlock Text="Nombre del conductor" Style="{StaticResource Etiqueta}"/>
            <TextBox x:Name="txtNombre"/>

            <TextBlock Text="Licencia" Style="{StaticResource Etiqueta}"/>
            <TextBox x:Name="txtLicencia"/>

            <TextBlock Text="Transporte" Style="{StaticResource Etiqueta}"/>
            <ComboBox x:Name="cboTransporte" IsEditable="True"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
            <Button Content="Guardar" Click="Guardar_Click" Margin="0,0,10,0"/>
            <Button Content="Volver" Style="{StaticResource BotonSecundario}" Click="Volver_Click"/>
        </StackPanel>
    </Grid>
</UserControl>

<UserControl x:Class="lab3.Views.AulasTablaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top">
            <TextBlock Text="Aulas (DataTable)" Style="{StaticResource Titulo}"/>
        </StackPanel>

        <DataGrid ItemsSource="{Binding Aulas}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Código" Binding="{Binding AulaId}" Width="90"/>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
                <DataGridTextColumn Header="Capacidad" Binding="{Binding Capacidad}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
